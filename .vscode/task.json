{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "build c or cpp",
        "type": "shell",
        "command": "sh",
        "args": [
          "-c",
          "if [[ '${file}' == *.c ]]; then gcc -std=c11 '${file}' -o '${fileDirname}/${fileBasenameNoExtension}'; else clang++ -std=c++17 '${file}' -o '${fileDirname}/${fileBasenameNoExtension}'; fi"
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": ["$gcc"],
        "detail": "Build C or C++ based on file extension"
      }
    ]
  }
  